# 作用：配置httpbin API测试环境的服务地址。

[servers]
url=https://httpbin.org

[http]
# 公共接口服务偶尔响应较慢，单次请求最多等待30秒。
timeout=30
# 正常环境必须校验TLS证书；仅本地自签名环境才应临时关闭。
verify=true
max_retries=2
pool_connections=10
pool_maxsize=10
# 响应编码错误默认抛出异常，避免损坏数据被静默替换；诊断场景可改为replace。
decode_errors=strict
